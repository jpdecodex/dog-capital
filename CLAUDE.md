# CLAUDE.md — dog-capitals

## What this repo does
Public marketing site for Dog Capital, built with Quarto and deployed to GitHub Pages at jpdecodex.github.io/dog-capitals. Showcases the systematic strategies (ES Core, SPY Core) with backtest research pages for Argentine investors.

## Current state (2026-07-11)

**Site restructured to a single scrolling page.** `index.qmd` is now one page — `#site-header` (icon + wordmark logo lockup, no descriptor/proof line — both were cut), `#site-nav` (5 pill links, real anchors: About Us / Programs / Research / Contact / Legal), then `.page-section` divs for `#about`, `#programs`, `#research`, `#contact`, `#legal`. Separate marketing pages are gone; only the deep program pages (`programs/*.qmd`) stay standalone.

**IA resolved: ES Core is Research, not a Program.** SPY Core — ETFs and S&P — CFD Futures (Axi, never named publicly) are the two cards under Programs — both are real/live-money vehicles. ES Core moved to Research as a short teaser linking to `programs/es-core.qmd`, since it's paper-only and not investable. Real reason ES Core stays paper-only: CME futures aren't BYMA-quoted like CEDEARs, so gains are taxed for Argentine investors (CEDEARs are capital-gains exempt) — confirmed by reading `es-core-vol-targeting/docs/PAPER.md` directly, not from an earlier (wrong) draft that blamed a "$250k capital requirement."

**New logo: icon + wordmark, separately typeset**, replacing the old flattened `LOGO-lockup.png` (dog fused into the wordmark's "O", hard to typeset around). `logo-icon.png` is the new icon asset — went through several PIL recoloring/dilation passes (`brand/logo-mark-v2.jpg` source → `brand/logo-mark-v2-green-thicker.png` final, intermediate iterations deleted) to get a strong, non-washed-out green at small header size. `LOGO-lockup.png` is now unused by the live site (kept in the repo, referenced only from `brand-manual.html` — cleanup candidate later, not touched this session).

**One font, site-wide: Georgia** (`body { font-family: Georgia, 'Times New Roman', serif; }`), replacing the earlier two/three-voice Playfair Display + IBM Plex Sans system. Matches the reference site (Core One Asset Management) the user pointed to. No Google Fonts import needed anymore — Georgia is a system font; the `include-in-header` font `<link>` tags were removed from `_quarto.yml`.

**Brand green changed to `#337e40`**, superseding brand-manual.html's documented `#2D7A3A` — sampled directly from the new icon's actual rendered color (alpha-composited against white) rather than trusting the old documented hex, which looked visibly paler than the icon in practice. Applied consistently to nav text, `h3.section-title`, and the `.tag.live` pill. **`brand-manual.html` was NOT updated to reflect this — still documents the old green and Playfair Display spec. Also: the working copy of this file was found overwritten with an unrelated bundler/export artifact (a self-extracting JS "unpacking" shell, not real content) at the end of this session and was reverted to its last-committed (stale) version rather than committed in that broken state. Updating the brand manual to match today's real decisions is still fully outstanding.**

**Program cards redesigned again**, this time to a sharper, plainer style per direct user feedback across several one-at-a-time rounds: white background, `1px solid #EBEBEB` border, **sharp corners (no border-radius)**, title reduced to body-size-but-bold (15px/700), the "LIVE TRADING" status tag simplified (word "Status" dropped, tag sized to match the title, vertically centered against it via `align-items:center` on `.card-header-row` and `line-height:1` on `.tag`), CTA text changed from "To Strategy" to "**To Program**". The stats table (Equity/CAGR/Sharpe/MaxDD) from the previous mockup-driven design was dropped entirely in this pass — current cards are title + status tag + one description paragraph + CTA link only.

**Known dead CSS, not cleaned up:** `.program-card .tag.paper` (color `#a8433f`) has no live user anymore — ES Core (the only "Paper Trading" card) no longer appears in Programs.

**Card descriptions rewritten and approved (2026-07-11)** after checking both strategy papers directly (`es-core-vol-targeting/docs/PAPER.md`, `spy-core-vol-targeting/docs/PAPER.md`) — an earlier draft claimed ES Core "requires capital above $250,000," which turned out to be wrong: that figure was from an unrelated, already-fixed IBKR paper-account contract-rounding bug (see `es-core-vol-targeting/CLAUDE.md` 2026-07-09 entries), not a real capital requirement. The actual, user-confirmed reason ES Core is paper-only: **CME futures aren't BYMA-quoted like CEDEARs, so gains are subject to Argentine capital gains tax** (CEDEARs are exempt — see `[[tax_context]]` memory) — plus the general (uncited, doesn't need a citation) fact that futures need more capital than ETFs structurally. Current copy reflects this; see `index.qmd`.

**Decision: do not credit "Core One Asset Management" (riskresearch.github.io, the user's friend's site) by name anywhere on the site**, at least not without the friend's explicit consent first — the user confirmed the strategy itself (not just general vol-targeting methodology) was adapted from that friend's work, which makes public attribution a relationship/consent question, not just a style choice. If acknowledging influence is wanted later, a generic non-named version ("methodology grounded in established vol-targeting research") was suggested as a safe alternative. This was already an open question in `[[dog_capital_site_architecture]]` from an earlier session — still not resolved, revisit only after the user has actually talked to their friend.

**Done this session (was "approved but not yet implemented" as of the last entry):** nav relabel to "About Us / Programs / Research / Contact / Legal" and the single-page jump-nav restructure — both landed, see "Current state" above.

**Correction: `programs/es-core.qmd` and `programs/spy-core.qmd` already have full content** (methodology, instrument rationale, backtest tables, equity/drawdown/Monte-Carlo charts) — landed 06-18/19 and 07-03 respectively, confirmed via `git log`. An earlier entry here claiming this content "still needs to land" was wrong; the long-form write-ups drafted in chat this session and pulled from the homepage's Research teaser were a redundant second draft of material that already existed on those pages, not a real gap.

**`programs/sp-cfd.qmd` written this session** — the S&P — CFD Futures program had no page at all (card said "Program page in progress"). New page follows the same framework/rationale structure as the other two program pages, grounded in the real EA parameters (VolTargetPct 0.20, MaxLeverage 3.0, 21d vol window, EMA(10) — read directly from `es-core-vol-targeting/execution/mt4/SP500_VolTarget.mq4`). **Deliberately carries no performance numbers** — no backtest table, no live-account figures — per the standing disclosure rule (`[[feedback_public_site_disclosure]]`: never name Axi/Darwinex/Francisco, never publish real balance- or entry-level-tied figures; the underlying Axi/prop-firm risk is flagged in memory as "not yet resolved"). The page states a real track record exists and predates the systematic EA, without naming the venue or citing figures — flag this scope choice to the user; publishing real percentage-return numbers here was deliberately deferred, not ruled out.

**Tooling note:** the `Claude_Browser` preview tool (`preview_screenshot`/`preview_eval`) was unreliable/hanging for most of this session (intermittent, sometimes fixed by restarting the server, sometimes not). The `claude-in-chrome` tool (real Chrome extension, `computer`/`javascript_tool`/`get_page_text`) worked reliably as a fallback for both visual screenshots and external-site research (used to research riskresearch.github.io, AQR, ReSolve, Cambria, Man Group for design comparison). Prefer `claude-in-chrome` first if `Claude_Browser` acts up again.

**Working process for this rebuild (explicit, 2026-07-10): slow, step by step, user-directed.** The user now sometimes pastes Notion copy directly, sometimes shares a live Notion URL to re-fetch (state changes between sessions — always re-fetch rather than trusting an earlier read in this same conversation, it may be stale). Don't write or edit any code speculatively, including for a section the user has already named as next — wait until they point at a specific piece and ask for it. User is also evaluating **Webflow** as a way to design visually and hand off real HTML/CSS instead of screenshots/mockups — not yet used, may come up next session.

## Next action
1. **Decide whether to publish real performance figures on `programs/sp-cfd.qmd`** — the page currently states a track record exists without citing it (see "Current state" above). Needs an explicit call, not an assumption, since it touches the still-unresolved Axi/prop-firm disclosure risk.
2. **Update `brand-manual.html`** to match today's real decisions: Georgia-only typography (drop the Playfair Display spec), `#337e40` green (not `#2D7A3A`), new icon+wordmark logo (not the old flattened `LOGO-lockup.png`), sharp-corner no-stats-table card style. Flagged twice now as outstanding — the file was also found corrupted (overwritten by an unrelated bundler artifact) and reverted at the end of this session, so it's still on the *previous* stale spec, not just missing the updates.
3. `favicon-32.png`/`favicon-180.png` are still old soft-edged crops from the flattened raster; `favicon-180.svg` (a proper vector trace of the mark) exists in the repo root but isn't wired up anywhere yet (`<link rel="apple-touch-icon">` etc. in `_quarto.yml`/`index.qmd` still point at the old PNGs). Low urgency.
4. Decide what to do with the now-unused `LOGO-lockup.png` / `docs/LOGO-lockup.png` (dead asset, only referenced from `brand-manual.html`) — delete or keep as historical reference.

## Architecture decisions
- Quarto + GitHub Pages (`docs/` build output), no JS framework — consistent with the rest of the ecosystem (see playbook ADR 005).
- Programs are separate `.qmd` pages under `programs/`, research write-ups under `research/`.
- Brand reference material (Claude Design manual, visual system spec) lives in top-level `brand/` — deliberately outside `docs/` (Quarto's output-dir gets swept on render; anything untracked there is at risk).
- Site scope is public-facing only. Venue names for the systematic programs (IBKR, IOL, Balanz) are fine to state — standard track-record transparency. Never: prop-firm/ladder mechanics, real balances, entry/exit levels, or partner names (those live in es-core-vol-targeting / spy-core-vol-targeting).
